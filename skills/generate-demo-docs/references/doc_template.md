# 文档输出模板 / Document Output Template

> 此模板是 `generate-docs` 命令生成 `PROJECT_EXPLANATION.md` 时必须遵守的骨架结构。
> 章节顺序固定，不能调换。每章节的必要元素均已标注。

---

```markdown
<!--
  PROJECT_EXPLANATION.md
  自动生成 by generate-demo-docs skill
  生成日期：{DATE}
  分析目录：{TARGET_DIR}
-->

# {项目名称} — 项目说明文档

> **文档类型**：项目说明文档（非 README）  
> **适合读者**：研发工程师 / 产品经理 / 测试人员 / AI 分析工具  
> **生成方式**：由 Claude Code `/generate-docs` 命令自动生成  
> **最后更新**：{DATE}

---

## 1. 项目总览 / Executive Summary

**项目名称**：{项目名称}  
**一句话定义**：{项目名称} 是一个 {类型}，用于 {核心用途}。

### 核心能力

- {能力 1}
- {能力 2}
- {能力 3}
（列出 3-7 个，每条一行，用动词开头）

### 适合谁阅读

| 角色 | 建议阅读章节 | 关注重点 |
|------|------------|---------|
| 研发工程师 | 全文 | 实现细节、调用关系、复杂脚本解读 |
| 产品经理 | 章节 1-3 | 功能能力、使用场景、能力边界 |
| 测试人员 | 章节 2、4 | 功能清单、输入输出格式 |
| AI 分析工具 | 章节 6-7 | 调用链、数据流、复杂实现细节 |

### 快速理解摘要

{100-200 字白话文。从"这个项目"开始写。解释：它做什么、怎么做、适合什么场景。
不要写"本文档旨在..."，直接描述项目本身。}

---

## 2. 功能清单 / Feature Breakdown

### {功能类别 A}（如：核心功能）

#### {功能名称 1}

| 字段 | 说明 |
|------|------|
| 对应脚本 | `path/to/script.py` |
| 功能作用 | {一句话说明} |
| 输入 | {参数类型、格式} |
| 输出 | {返回值 / 文件 / 副作用} |
| 适用场景 | {什么时候用这个功能} |
| 依赖模块 | {是/否，列出名称} |

{1-3 句补充说明}

#### {功能名称 2}
{同上格式}

### {功能类别 B}（如：辅助功能）
{同上}

### {功能类别 C}（如：工具功能）
{同上}

---

## 3. 文件与脚本地图 / Project File Map

```
{项目根目录}/
├── {文件名}              ← [{标签}] {一句话用途}
├── {目录名}/
│   ├── {文件名}          ← [{标签}] {一句话用途}
│   └── {文件名}          ← [{标签}] {一句话用途}
...
```

**标签说明**：
- `[主入口]` — 程序主入口，从这里开始执行
- `[核心逻辑]` — 核心业务实现
- `[工具脚本]` — 通用辅助函数集合
- `[配置文件]` — 运行时参数和环境配置
- `[测试]` — 单元测试或集成测试
- `[数据/样例]` — 示例数据、测试用例
- `[文档]` — 说明文档
- `[辅助脚本]` — 辅助完成某项任务的次要脚本

**关键文件快速索引**：

| 文件 | 标签 | 一句话用途 |
|------|------|-----------|
| {文件路径} | {标签} | {用途} |
{重复...}

---

## 4. 脚本能力说明 / What Each Script Can Do

> 按重要性排序：主入口 > 核心逻辑 > 被多处依赖的模块 > 辅助脚本

### `{path/to/main_script.py}` ⭐ 主入口

**这个脚本是干什么的**

{1-2 段白话文解释。不要照搬函数名，解释业务意图。}

**它可以完成哪些事情**

- {能力 1}
- {能力 2}
- {能力 3}

**如何调用**

```bash
# 基本用法
python {script_name}.py {参数示例}

# 完整参数
python {script_name}.py --{arg1} {val1} --{arg2} {val2}
```

或（如果是模块导入方式）：
```python
from {module} import {function}
result = {function}({example_args})
```

**典型输入输出示例**

输入：`{示例输入}`  
输出：`{示例输出}`

**成功运行后会产生什么**

- {文件 / 数据 / 副作用 1}
- {文件 / 数据 / 副作用 2}

**注意事项**

- {前置条件}
- {常见错误}
- {环境要求}

---

### `{path/to/another_script.py}`

{同上格式，根据重要程度决定详细程度}

---

## 5. 优缺点分析 / Strengths and Limitations

> 客观分析，不只夸优点。

### 整体项目评估

#### 优点

- {优点 1}
- {优点 2}
- {优点 3}

#### 局限性

- {局限 1}
- {局限 2}

#### 潜在风险

- {风险 1}（风险等级：高/中/低）
- {风险 2}

#### 可维护性

⭐⭐⭐☆☆（3/5）

{理由：2-3 句，指出具体问题}

#### 可扩展性

⭐⭐⭐⭐☆（4/5）

{理由：2-3 句}

#### 最值得重构的地方

1. `{文件/函数}`：{原因} → {建议方向}
2. `{文件/函数}`：{原因} → {建议方向}
3. `{文件/函数}`：{原因} → {建议方向}

### 关键脚本单独评估

#### `{script_name}`

| 维度 | 评分 | 说明 |
|------|------|------|
| 代码质量 | ⭐⭐⭐☆☆ | {原因} |
| 错误处理 | ⭐⭐☆☆☆ | {原因} |
| 可读性 | ⭐⭐⭐⭐☆ | {原因} |
| 扩展性 | ⭐⭐⭐☆☆ | {原因} |

**建议重构方向**：{具体建议}

---

## 6. 内部调用与实现逻辑 / Internal Flow and Call Graph

### 主流程步骤

```
Step 1: {用户触发 / 程序启动}
  └─ {触发方式：命令行 / API 调用 / 定时任务 / 事件驱动}

Step 2: {初始化阶段}
  └─ {加载配置}
  └─ {初始化依赖}
  └─ {验证参数}

Step 3: {预处理阶段}
  └─ {数据准备}
  └─ {资源分配}

Step 4: {核心处理阶段}
  └─ {主要业务逻辑}

Step 5: {后处理阶段}
  └─ {结果整理}
  └─ {输出 / 写文件}

Step 6: {清理阶段}
  └─ {资源释放}
  └─ {错误处理}
```

### 调用链

```
{main 函数/入口}()
  └─ {init 相关函数}()
  └─ {配置加载函数}()
  └─ {核心处理类}.__init__()
       └─ {依赖初始化}()
  └─ {核心处理类}.{主方法}()
       └─ {子函数 1}() [{文件名}]
       └─ {子函数 2}() [{文件名}]
            └─ {更深层的调用}() [{文件名}]
  └─ {输出函数}()
```

### 数据流

```
输入：{输入数据描述}
  ↓
{数据转换 1 步骤}（{使用的函数/类}）
  ↓
{数据转换 2 步骤}（{关键数据结构}）
  ↓
{数据转换 3 步骤}
  ↓
输出：{输出数据描述}
```

### 外部资源调用

| 资源类型 | 资源名称 | 调用位置 | 说明 |
|---------|---------|---------|------|
| API | {API 名称} | `{文件:函数}` | {用途} |
| 文件系统 | `{路径}` | `{文件:函数}` | {读/写，用途} |
| 环境变量 | `{ENV_VAR}` | `{文件}` | {必须/可选，默认值} |
{如无外部依赖，写"无外部资源调用"}

### 关键数据结构

```python
# {数据结构名称}
{
  "field1": "...",  # {说明}
  "field2": "...",  # {说明}
}
```

---

## 7. 复杂脚本深度解读 / Deep Technical Notes for AI and Maintainers

> 本章面向维护者和 AI 工具，提供足够深的技术细节，支持后续代码分析和改造。

### `{path/to/complex_script.py}` — 深度解读

#### 模块概述

{3-5 句，说明：整体职责是什么 / 在整个项目中的地位 / 与其他模块的关系}

#### 主要类与职责

| 类名 | 职责 | 关键方法 |
|------|------|---------|
| `{ClassName}` | {职责描述} | `{method1()}`, `{method2()}` |
{如无类（纯函数脚本），说明主要函数列表}

#### 关键函数说明

##### `{function_name}({param1}: {type}, {param2}: {type}) -> {return_type}`

- **职责**：{做什么}
- **参数**：
  - `{param1}`：{说明，包括格式、范围、默认值}
  - `{param2}`：{说明}
- **返回值**：{说明}
- **副作用**：{修改了什么全局状态 / 写了什么文件 / 发了什么请求}
- **调用关系**：被 `{caller}` 调用，内部调用了 `{callee1}`, `{callee2}`
- **注意**：{容易踩坑的地方}

{重复，覆盖 3-5 个最关键的函数}

#### 核心算法 / 处理流程

{用步骤列表解释核心算法，不要原样贴代码}

```
1. {步骤 1}：{说明}
2. {步骤 2}：{说明}
3. {步骤 3}：{说明}
```

#### 容易看不懂的代码段

##### 代码段 1：{简短描述}

**代码位置**：第 {N} 行附近

**这段代码的意图**：{用白话解释，说清楚为什么这样写，不只是说做了什么}

**为什么不用更简单的写法**：{如果有特殊原因}

{重复 2-5 处}

#### 隐式约定与假设

- **路径约定**：{脚本假设什么路径存在，相对路径是从哪里算的}
- **数据格式**：{输入数据的隐含格式要求}
- **环境依赖**：{必须安装哪些包，版本要求}
- **执行顺序**：{如果有必须先调用某函数的约定}
- **线程安全**：{是否线程安全，有没有全局状态}
- **内存注意**：{大文件处理、内存释放的注意事项}

#### 维护者建议

1. **修改前必读**：{在改这个脚本之前，必须理解哪些前置知识}
2. **最容易出错的地方**：{哪个函数 / 哪个逻辑最脆弱}
3. **测试建议**：{改动后如何验证正确性}
4. **扩展建议**：{如果要增加新功能，最合适从哪里改起}

---

{重复章节 7 的结构，覆盖所有复杂脚本}

---

*文档由 generate-demo-docs skill v1.0.0 自动生成*  
*生成时间：{DATE}*  
*如发现文档有误或信息过时，请重新运行 `/generate-docs` 更新*
```
