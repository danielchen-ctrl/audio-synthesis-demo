# demo 目录说明

当前 `demo/` 里放两类内容：

- 按时间戳生成的任务目录
  - 每个目录对应一次在线生成音频任务
  - 常见文件包括 `manifest.json`、生成的文本、生成的音频
- `对话情景参数/`
  - 当前 demo 仍会用到的场景参数源文件

## 说明

- 这里是运行产物区，不建议手工整理单个任务目录
- 如果需要清理历史任务，优先通过页面里的“删除”或“清理过时任务”来做
- `tts_input_debug.txt` 属于调试残留，如果不再需要，可以后续继续清理策略统一处理
